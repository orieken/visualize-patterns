/**
 * CHALLENGE: Implement Quick Sort.
 * 
 * Rules:
 * 1. Pick a pivot.
 * 2. Partition array into 'left' (smaller than pivot) and 'right' (larger).
 * 3. Recursively sort left and right.
 * 4. Combine: [...left, pivot, ...right]
 */
export function quickSortChallenge(arr: number[]): number[] {
  // TODO: Base case
  
  // TODO: Partition
  
  // TODO: Recurse and combine
  
  return []; // <-- FIX THIS
}
