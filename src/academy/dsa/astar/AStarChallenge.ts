import { Graph } from './AStar';

/**
 * CHALLENGE:
 * Implement A* Search.
 * 
 * Rules:
 * 1. Maintain gScore (cost from start) and fScore (gScore + heuristic).
 * 2. Use a heuristic function (e.g. Manhattan distance).
 * 3. Always expand the node with lowest fScore.
 */
export function aStarChallenge(graph: Graph, startId: string, endId: string): string[] {
  // TODO: Initialize scores and open set
  
  // TODO: Loop to find path
  
  return []; // <-- FIX THIS
}
