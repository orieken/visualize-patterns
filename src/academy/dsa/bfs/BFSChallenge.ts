/**
 * CHALLENGE:
 * Implement Breadth-First Search.
 * 
 * Rules:
 * 1. Visit neighbors level by level.
 * 2. Use a Queue (FIFO).
 * 3. Track visited nodes to avoid cycles.
 */
export function bfsChallenge(graph: Record<string, string[]>, start: string): string[] {
  // TODO: Initialize your Queue and Visited Tracking
  
  // TODO: Loop while queue is not empty
  
  // Hint: queue.shift() removes from front
  // Hint: queue.push() adds to back
  
  return []; // <-- FIX THIS
}
