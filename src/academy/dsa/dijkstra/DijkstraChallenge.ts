/**
 * CHALLENGE:
 * Implement Dijkstra's Algorithm.
 * 
 * Rules:
 * 1. Initialize distances to Infinity, start to 0.
 * 2. Use a Priority Queue (or simple loop) to pick the closest unvisited node.
 * 3. Relax edges: If (dist[u] + weight(u,v) < dist[v]), update dist[v].
 */
export function dijkstraChallenge(
  graph: Record<string, Record<string, number>>,
  start: string
): Record<string, number> {
  // TODO: Init distances
  
  // TODO: Loop until all visited
  
  return {}; // <-- FIX THIS
}
