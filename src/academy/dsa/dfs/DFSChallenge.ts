/**
 * CHALLENGE:
 * Implement Depth-First Search.
 * 
 * Rules:
 * 1. Visit neighbors by going deep into one branch before backtracking.
 * 2. Use recursion (Call Stack) or an explicit Stack (LIFO).
 * 3. Track visited nodes to avoid cycles.
 */
export function dfsChallenge(graph: Record<string, string[]>, start: string): string[] {
  // TODO: Create a visited Set
  // TODO: Create a helper function for recursion OR use a stack loop
  
  return []; // <-- FIX THIS
}
