/**
 * CHALLENGE: Implement Topological Sort.
 * 
 * Rules:
 * 1. Calculate in-degrees.
 * 2. Enqueue nodes with 0 in-degree.
 * 3. Process queue: remove node, add to result, decrement neighbors.
 * 4. If neighbor becomes 0, enqueue it.
 */
export function topologicalSortChallenge(graph: Record<string, string[]>): string[] {
  // TODO: Implement Kahn's Algorithm
  
  return []; // <-- FIX THIS
}
