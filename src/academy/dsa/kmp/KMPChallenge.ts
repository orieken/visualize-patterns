/**
 * CHALLENGE: Implement KMP.
 * 
 * Rules:
 * 1. Compute LPS (Longest Prefix Suffix) array.
 * 2. Use indices i (text) and j (pattern).
 * 3. Use LPS to skip comparisons on mismatch.
 */
export function kmpSearchChallenge(text: string, pattern: string): number[] {
  // TODO: LPS
  
  // TODO: Search loop
  
  return []; // <-- FIX THIS
}
